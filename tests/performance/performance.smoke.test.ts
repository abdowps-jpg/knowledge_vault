import { describe, it, expect } from "vitest";

describe.skip("performance smoke", () => {
  it("placeholder benchmark contract", () => {
    // TODO: add real launch/search/query benchmarks and thresholds.
    expect(true).toBe(true);
  });
});
